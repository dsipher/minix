.code16

	pushw %ax
	pushw %dx
	pushw %cx
	pushw %ax
	pushw $0x200
	pushw $0xffff
	pushw %cs
	pushw %ds
	pushw %ss
	pushw %es
	pushw %gs
	pushw %fs
	pushl %es

.code32
	pushl %eax
	pushl %ebp
	pushl %ebx
	pushl %ecx
	pushl %edi
	pushl %edx
	pushl %esi
	pushl (%eax)
	pushl $-1
	pushl $0xfffffffe
	pushl $0x7fffffff
	pushw %cs
	pushw %fs
	pushw %ds
	pushw %gs
	pushw %ss
	pushw %es
	pushl %es

.code64

	pushq $0
	pushq $1
	pushq $2
	pushq $28
	pushq $3
	pushw $0
	pushw $1
	pushw $2
	pushw $28
	pushw $3
	pushq $375390250
	pushq $4
	pushq %r12
	pushq %r13
	pushq %r14
	pushq %r15
	pushq %r8
	pushq %rax
	pushq %rbp
	pushq %rbx
	pushq %rcx
	pushq %rdi
	pushq %rdx
	pushq %rsi
	pushq (%r12)
	pushq (%r13)
	pushq (%rax)
	pushq (%rax,%rcx,8)
	pushq (%rbx)
	pushq (%rbx,%rax,8)
	pushq (%rcx,%rax,8)
	pushq (%rcx,%rdx,8)
	pushq (%rdi)
	pushq (%rdx,%rcx,8)
	pushq -16(%rbp)
	pushq -24(%rbp)
	pushq -32(%rbp)
	pushq -48(%rbp)
	pushq -8(%rbp)
	pushq -8(%rbx)
	pushq 16(%r12)
	pushq 16(%r14)
	pushq 16(%rbp)
	pushq 16(%rbx)
	pushq 16(%rcx)
	pushq 24(%rbx)
	pushq 8(%r12)
	pushq 8(%rbx)
	pushq 8(%rcx)
	pushq %gs
	pushq %fs
